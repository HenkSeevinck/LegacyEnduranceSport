import 'package:flutter/material.dart';

class TrainingFocus extends StatelessWidget {
  const TrainingFocus({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Training Focus Setup', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),
      ),
    );
  }
}
